# 实现左侧树形导航结构

## 1. 修改数据库 schema
- 在 `packages/db/prisma/schema/schema.prisma` 中为 `Page` 和 `Resource` 表添加 `isExpanded` 字段
- 字段类型：`Boolean`，默认值：`false`

## 2. 更新 page 接口
- 修改 `packages/api/src/routers/page.ts` 中的 `getAll` 方法
- 返回树形结构数据，包含嵌套的 `children` 数组
- 确保返回 `isExpanded` 字段

## 3. 更新左侧侧边栏组件
- 修改 `apps/web/src/components/layout/sidebar-left.tsx`
- 使用 shadcn 组件构建树形结构
- 添加展开/折叠功能
- 实现页面列表的树形展示

## 4. 添加必要的 shadcn 组件
- 如果需要，添加树形相关的 shadcn 组件
- 确保组件与现有设计风格一致

## 5. 测试和验证
- 确保树形结构正确显示
- 确保展开/折叠状态可以保存
- 确保页面导航正常工作

所有实现将遵循最小化 MVP 开发原则，使用最小的代码量完成功能。