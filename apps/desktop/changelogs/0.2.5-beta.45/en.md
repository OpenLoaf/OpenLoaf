## What's New

### 📦 SDK Upgrade
- Upgraded `@openloaf-saas/sdk` from 0.1.35 to 0.1.37
- Adopted new `locale` parameter in SaaSClient (replaces manual `Accept-Language` header)
- Simplified skill market API calls: `detail()` and `download()` no longer need explicit `lang` parameter
