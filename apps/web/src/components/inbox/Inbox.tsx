"use client";

export default function InboxPage({
  panelKey: _panelKey,
  tabId: _tabId,
}: {
  panelKey: string;
  tabId: string;
}) {
  return (
    <div className="h-full w-full">
      <div className="text-sm text-muted-foreground">收集箱（开发中）</div>
    </div>
  );
}

