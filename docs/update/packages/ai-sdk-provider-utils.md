# @ai-sdk/provider-utils

升级：`4.0.21` → `4.0.23`

## 变更摘要

- `fix: allow inline data URLs in download validation` —— 允许 inline data URL 通过下载校验（避免被误判为非法 URL）。
- `chore(provider-utils,google): fix grammar errors in error and warning messages` —— 修复错误/警告消息中的语法问题。

## 原始 changelog 摘录

### 4.0.23
- chore(provider-utils,google): fix grammar errors in error and warning messages

### 4.0.22
- fix: allow inline data URLs in download validation
